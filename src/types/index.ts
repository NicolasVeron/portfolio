export type ProjectTypes = {
    name: string
    image: string
    icon: string
    description: string
    skills: string
    deploy: string
    repository: string
}

type ValueText = {
    value: boolean
    text: string
}